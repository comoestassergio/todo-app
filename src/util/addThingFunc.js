const thing = {
    create: function(title, desc, dueDate, priority){
        const title = title
        const desc = desc 
        const dueDate = dueDate
        const priority = priority

        return {title, desc, dueDate, priority}
    }
}